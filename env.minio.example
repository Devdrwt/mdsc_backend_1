# MinIO Configuration
# Copiez ces variables dans votre fichier .env

# Endpoint MinIO (hostname ou IP)
MINIO_ENDPOINT=localhost

# Port MinIO
MINIO_PORT=9000

# Utiliser SSL/TLS (true/false)
MINIO_USE_SSL=false

# Clés d'accès MinIO
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Nom du bucket pour stocker les fichiers
MINIO_BUCKET_NAME=mdsc-files

# Région (optionnel, par défaut us-east-1)
MINIO_REGION=us-east-1

# URL publique personnalisée pour MinIO (optionnel)
# Utilisez cette variable si MinIO est accessible via un domaine différent
# Exemple: https://cdn.yourdomain.com
# MINIO_PUBLIC_URL=http://your-minio-domain.com
